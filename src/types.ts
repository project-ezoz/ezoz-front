export interface MarkerType {
  title: string;
  latlan: string;
  contents: string;
}

export interface CenterMarkerType {
  lat: number;
  lng: number;
}

export interface PlaceType {
  id: number;
  title: string;
  lat: string;
  lng: string;
  content: string;
}
